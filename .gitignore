# Secrets — never commit
.env
.env.*
!.env.example

# Python
__pycache__/
*.pyc
.pytest_cache/
*.egg-info/
.venv/
venv/

# Node / frontend (web/ comes in Phase 6b)
node_modules/
web/dist/
web/.vite/

# OS
.DS_Store

# Generated artifacts (regenerated by eval/ scripts)
submissions/

# Discord absence store (runtime state)
api/_absences.json
api/_notes.json
bot/_registrations.json

# Mutable app state behind the persistence adapter (runtime; FileStore under state/).
# Keep the reviewed belief_extractions.json + README.md committed; ignore the runtime blobs.
state/notes.json
state/absences.json
state/graph_snapshot.json
